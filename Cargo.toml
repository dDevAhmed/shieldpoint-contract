[workspace]
resolver = "2"
members = [
    "contracts/verifier",
    "contracts/registry",
    "contracts/auth",
]

[workspace.dependencies]
soroban-sdk = "25.3.1"
stellar-xdr = "26.0.1"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
debug = false
panic = "abort"
strip = "symbols"
